// Generated by CoffeeScript 2.7.0
(function() {
  var Analyzer, AptanaSch, BashParser, CParser, CSharpParser, CodePainter, CodeReader, CoffeeScriptParser, CppCommentAnalyzer, CppCommentPlusAnalyzer, CppParser, CssParser, DelimiterAnalyzer, DraculaSch, Factory, GitHubDarkSch, GitHubLightSch, GoParser, HtmlParser, IntelliJLightSch, JavaParser, JavaScriptParser, JsonParser, KotlinParser, MonokaiSch, NewLineAnalyzer, NordSch, OneDarkSch, PaddingAnalyzer, Painter, PainterFactory, PainterFactoryController, PaintersBase, ParserFactory, PhpParser, PythonParser, RubyParser, RustParser, Scheme, SchemeDescription, SchemeFactory, SchemeFactoryController, ServicePainterBorder, SharpDirectiveAnalyzer, SnipController, SolarizedDarkSch, SolarizedLightSch, SpaceAnalyzer, SqlLineCommentAnalyzer, SqlParser, StringAnalyzer, SwiftParser, SyntaxParser, SyntaxToken, TokenComment, TokenDelimiter, TokenKeyword, TokenNewLine, TokenNormal, TokenOutlined, TokenPadding, TokenSharpDirective, TokenSpace, TokenString, TokenUndefined, TypeScriptParser, UnixCommentAnalyzer, VisualStudioTenSch, XcodeLightSch, XmlParser, YamlParser, autoLoader, base, base1, base2, base3, base4, root;

  if ((base = Array.prototype).some == null) {
    base.some = function(f) {
      var i, len, ref, x;
      ref = this;
      for (i = 0, len = ref.length; i < len; i++) {
        x = ref[i];
        if (f(x)) {
          return true;
        }
      }
      return false;
    };
  }

  if ((base1 = Array.prototype).every == null) {
    base1.every = function(f) {
      var i, len, ref, x;
      ref = this;
      for (i = 0, len = ref.length; i < len; i++) {
        x = ref[i];
        if (!f(x)) {
          return false;
        }
      }
      return true;
    };
  }

  if ((base2 = String.prototype).toImg == null) {
    base2.toImg = function() {
      var img;
      img = document.createElement("img");
      img.src = this;
      return img;
    };
  }

  if ((base3 = String.prototype).trim == null) {
    base3.trim = function() {
      return this.replace(/^\s+|\s+$/g, '');
    };
  }

  if ((base4 = String.prototype).dashToCamel == null) {
    base4.dashToCamel = function() {
      return this.replace(/\W+(.)/g, function(x, chr) {
        return chr.toUpperCase();
      });
    };
  }

  SyntaxToken = class SyntaxToken {
    constructor(type = '(null)', content1 = '') {
      this.type = type;
      this.content = content1;
    }

    code() {
      return this.type;
    }

    addChar(chr) {
      return this.content += chr;
    }

    toStr() {
      return this.type.toString() + ': ' + this.content.toString();
    }

  };

  TokenUndefined = class TokenUndefined extends SyntaxToken {
    constructor(content = '') {
      super('undefined', content);
    }

  };

  TokenSpace = class TokenSpace extends SyntaxToken {
    constructor(content = '') {
      super('space', content);
    }

  };

  TokenNewLine = class TokenNewLine extends SyntaxToken {
    constructor(content = '') {
      super('new-line', content);
    }

  };

  TokenDelimiter = class TokenDelimiter extends SyntaxToken {
    constructor(content = '') {
      super('delimiter', content);
    }

  };

  TokenComment = class TokenComment extends SyntaxToken {
    constructor(content = '') {
      super('comment', content);
    }

  };

  TokenPadding = class TokenPadding extends SyntaxToken {
    constructor(content = '') {
      super('padding', content);
    }

  };

  TokenString = class TokenString extends SyntaxToken {
    constructor(content = '') {
      super('string', content);
    }

  };

  TokenKeyword = class TokenKeyword extends SyntaxToken {
    constructor(content = '') {
      super('keyword', content);
    }

  };

  TokenNormal = class TokenNormal extends SyntaxToken {
    constructor(content = '') {
      super('normal', content);
    }

  };

  TokenOutlined = class TokenOutlined extends SyntaxToken {
    constructor(content = '') {
      super('outlined', content);
    }

  };

  TokenSharpDirective = class TokenSharpDirective extends SyntaxToken {
    constructor(content = '') {
      super('directive', content);
    }

  };

  SchemeDescription = class SchemeDescription {
    constructor(color = '#000000', param = 'normal') {
      this.color = color;
      this.param = param;
    }

  };

  Factory = class Factory {
    constructor() {
      this.data = [];
    }

    Register(code, item, title = null) {
      var p;
      p = {};
      p.code = code;
      p.item = item;
      if ((title != null)) {
        p.title = title;
      } else {
        p.title = code;
      }
      this.data.push(p);
      return true;
    }

    Find(code) {
      var i, len, p, ref;
      ref = this.data;
      for (i = 0, len = ref.length; i < len; i++) {
        p = ref[i];
        if (p.code === code) {
          return p.item;
        }
      }
      return null;
    }

  };

  CodeReader = class CodeReader {
    constructor(data) {
      this.data = data;
      this.index = 0;
      this.buffer = '';
    }

    current() {
      return this.data.substring(this.index, this.index + 1);
    }

    fetchPrev(offset = 0) {
      if (0 <= this.index - offset) {
        return this.data.substring(this.index - offset, this.index - offset + 1);
      }
      return null;
    }

    skip(offset) {
      var newIndex;
      newIndex = this.index + offset;
      if (newIndex >= 0 && newIndex < this.data.length) {
        return this.index = newIndex;
      }
    }

    fetchNext(offset = 0) {
      if (this.data.length > this.index + offset) {
        return this.data.substring(this.index + offset, this.index + offset + 1);
      }
      return null;
    }

    readNext() {
      if (this.index + 1 >= this.data.length) {
        return false;
      }
      this.index += 1;
      return true;
    }

    readUntil(condition, withLast = true) {
      this.buffer = '';
      while (this.readNext()) {
        if (condition(this)) {
          if (withLast) {
            this.buffer += this.current();
          } else {
            this.index--;
          }
          return true;
        } else {
          this.buffer += this.current();
        }
      }
      return this.buffer += this.current();
    }

    readUntilChars() {
      var args;
      args = Array.prototype.slice.call(arguments);
      return this.readUntil(function(x) {
        return args.some(function(v) {
          return x.current() === v;
        });
      });
    }

    readUntilCharsLast(withLast) {
      var args;
      args = Array.prototype.slice.call(arguments);
      args = args.slice(1);
      return this.readUntil((function(x) {
        return args.some(function(v) {
          return x.current() === v;
        });
      }), withLast);
    }

  };

  Analyzer = class Analyzer {
    match(reader, temporaryToken, tokens) {
      return false;
    }

    performAction(temporaryToken, reader, tokens, flush) {
      flush(temporaryToken);
      temporaryToken = null;
      flush(new TokenUndefined());
      return temporaryToken;
    }

  };

  NewLineAnalyzer = class NewLineAnalyzer extends Analyzer {
    match(reader, temporaryToken, tokens) {
      var ch;
      ch = reader.current();
      return ch === "\r" || ch === "\n";
    }

    opposite(c) {
      var ref;
      return (ref = c === "\r") != null ? ref : {
        "\n": "\r"
      };
    }

    performAction(temporaryToken, reader, tokens, flush) {
      flush(temporaryToken);
      temporaryToken = null;
      flush(new TokenNewLine());
      if (reader.fetchNext() === this.opposite(reader.current())) {
        reader.skip(1);
      }
      return temporaryToken;
    }

  };

  SpaceAnalyzer = class SpaceAnalyzer extends Analyzer {
    match(reader, temporaryToken, tokens) {
      return reader.current() === ' ';
    }

    performAction(temporaryToken, reader, tokens, flush) {
      flush(temporaryToken);
      temporaryToken = null;
      flush(new TokenSpace());
      return temporaryToken;
    }

  };

  PaddingAnalyzer = class PaddingAnalyzer extends Analyzer {
    match(reader, temporaryToken, tokens) {
      return reader.current() === "\t";
    }

    performAction(temporaryToken, reader, tokens, flush) {
      flush(temporaryToken);
      temporaryToken = null;
      flush(new TokenPadding());
      return temporaryToken;
    }

  };

  StringAnalyzer = class StringAnalyzer extends Analyzer {
    match(reader, temporaryToken, tokens) {
      var ch;
      ch = reader.current();
      return ch === '"' || ch === "'";
    }

    performAction(temporaryToken, reader, tokens, flush) {
      var buff, ch;
      flush(temporaryToken);
      temporaryToken = null;
      ch = reader.current();
      buff = ch;
      while (true) {
        if (!reader.readUntilChars(ch)) {
          buff += reader.buffer;
          break;
        }
        buff += reader.buffer;
        if (buff.length === 2) {
          break;
        }
        if (buff.substring(buff.length - 2, buff.length - 1) === "\\" && buff.substring(buff.length - 3, buff.length - 2) !== "\\") {
          continue;
        }
        break;
      }
      flush(new TokenString(buff));
      return temporaryToken;
    }

  };

  DelimiterAnalyzer = class DelimiterAnalyzer extends Analyzer {
    constructor(delimeters = null) {
      super();
      this.delimeters = delimeters;
      if (this.delimeters === null) {
        this.delimeters = ['{', '}', '[', ']', '=', ';', ':', '?', '(', ')', ',', '<', '>', '.'];
      }
    }

    match(reader, temporaryToken, tokens) {
      var c;
      c = reader.current();
      return this.delimeters.some(function(x) {
        return c === x;
      });
    }

    performAction(temporaryToken, reader, tokens, flush) {
      flush(temporaryToken);
      temporaryToken = null;
      flush(new TokenDelimiter(reader.current()));
      return temporaryToken;
    }

  };

  CppCommentAnalyzer = class CppCommentAnalyzer extends Analyzer {
    match(reader, temporaryToken, tokens) {
      return temporaryToken === null && reader.current() === '/' && reader.fetchNext() === '/';
    }

    performAction(temporaryToken, reader, tokens, flush) {
      reader.readUntilCharsLast(false, "\r", "\n");
      flush(new TokenComment("/" + reader.buffer));
      return temporaryToken;
    }

  };

  CppCommentPlusAnalyzer = class CppCommentPlusAnalyzer extends Analyzer {
    match(reader, temporaryToken, tokens) {
      return temporaryToken === null && reader.current() === '/' && reader.fetchNext() === '*';
    }

    performAction(temporaryToken, reader, tokens, flush) {
      reader.readUntil((function(x) {
        return x.current() === '*' && x.fetchNext() === '/';
      }));
      flush(new TokenComment("/" + reader.buffer + "/"));
      return temporaryToken;
    }

  };

  UnixCommentAnalyzer = class UnixCommentAnalyzer extends Analyzer {
    match(reader, temporaryToken, tokens) {
      return temporaryToken === null && reader.current() === '#';
    }

    performAction(temporaryToken, reader, tokens, flush) {
      reader.readUntilCharsLast(false, "\r", "\n");
      flush(new TokenComment("#" + reader.buffer));
      return temporaryToken;
    }

  };

  SyntaxParser = class SyntaxParser {
    constructor() {
      this.outlinedWords = [];
      this.analyzers = [];
      this.keywords = [];
      this.registerAnalyzer = function(analyzer) {
        return this.analyzers.push(analyzer);
      };
      this.registerAnalyzers = function(analyzers) {
        var analyzer, i, len, results;
        results = [];
        for (i = 0, len = analyzers.length; i < len; i++) {
          analyzer = analyzers[i];
          results.push(this.registerAnalyzer(analyzer));
        }
        return results;
      };
      this.registerKeyword = function(keyword) {
        return this.keywords.push(keyword);
      };
      this.registerKeywords = function(keywords) {
        var i, keyword, len, results;
        results = [];
        for (i = 0, len = keywords.length; i < len; i++) {
          keyword = keywords[i];
          results.push(this.registerKeyword(keyword));
        }
        return results;
      };
      this.registerOutlinedWord = function(word) {
        return this.outlinedWords.push(word);
      };
      this.registerOutlinedWords = function(words) {
        var i, len, results, word;
        results = [];
        for (i = 0, len = words.length; i < len; i++) {
          word = words[i];
          results.push(this.registerOutlinedWord(word));
        }
        return results;
      };
      this.beforeTokenFlush = function(tokens, token) {
        var content;
        if (token instanceof TokenUndefined) {
          content = token.content;
          if (this.keywords.some(function(x) {
            return content === x;
          })) {
            return new TokenKeyword(content);
          }
          if (this.outlinedWords.some(function(x) {
            return x === content;
          })) {
            return new TokenOutlined(content);
          } else {
            return new TokenNormal(content);
          }
        } else {
          return token;
        }
      };
    }

    init() {
      this.registerAnalyzer(new NewLineAnalyzer());
      this.registerAnalyzer(new SpaceAnalyzer());
      this.registerAnalyzer(new PaddingAnalyzer());
      return this.registerAnalyzer(new StringAnalyzer());
    }

    tokenize(reader) {
      var flusher, temporary, tokens;
      tokens = new Array();
      flusher = (token) => {
        if (token === null) {
          return;
        }
        token = this.beforeTokenFlush(tokens, token);
        return tokens.push(token);
      };
      temporary = null;
      temporary = this.analyzeState(temporary, reader, tokens, flusher);
      while (reader.readNext()) {
        temporary = this.analyzeState(temporary, reader, tokens, flusher);
      }
      flusher(temporary);
      if (tokens.length > 0 && !(tokens[tokens.length - 1] instanceof TokenNewLine)) {
        tokens.push(new TokenNewLine());
      }
      return tokens;
    }

    analyzeState(temporary, reader, tokens, flusher) {
      var analyzer, i, len, ref;
      ref = this.analyzers;
      for (i = 0, len = ref.length; i < len; i++) {
        analyzer = ref[i];
        if (!analyzer.match(reader, temporary, tokens)) {
          continue;
        }
        temporary = analyzer.performAction(temporary, reader, tokens, flusher);
        if (temporary == null) {
          temporary = null;
        }
        return temporary;
      }
      if (temporary == null) {
        temporary = new TokenUndefined();
      }
      temporary.addChar(reader.current());
      return temporary;
    }

  };

  ParserFactory = (function() {
    class ParserFactory {
      static Register(exts, parserName, title) {
        var ext, i, len, p;
        this.list.push({
          name: title,
          code: exts[0]
        });
        for (i = 0, len = exts.length; i < len; i++) {
          ext = exts[i];
          p = {};
          p.ext = ext.toLowerCase();
          p.parser = parserName;
          p.title = title;
          this.parsers.push(p);
        }
        return true;
      }

      static findParser(extension) {
        var ext, i, len, p, ref;
        ext = extension.toLowerCase();
        ref = this.parsers;
        for (i = 0, len = ref.length; i < len; i++) {
          p = ref[i];
          if (p.ext === ext) {
            return p.parser;
          }
        }
        return null;
      }

      static makePrintable(arr) {
        arr.Print = function() {
          var i, len, ref, s, t;
          s = '';
          ref = this;
          for (i = 0, len = ref.length; i < len; i++) {
            t = ref[i];
            s += t.toStr() + "\r\n";
          }
          return s;
        };
        return arr;
      }

      static List() {
        return this.list;
      }

      static Parse(ext, text) {
        var c, p;
        c = new CodeReader(text);
        p = ParserFactory.findParser(ext);
        if (p === null) {
          return null;
        }
        p = new p();
        p.init();
        return ParserFactory.makePrintable(p.tokenize(c));
      }

    };

    ParserFactory.parsers = [];

    ParserFactory.list = [];

    return ParserFactory;

  }).call(this);

  SharpDirectiveAnalyzer = class SharpDirectiveAnalyzer extends Analyzer {
    match(reader, temporaryToken, tokens) {
      return temporaryToken === null && reader.current() === '#';
    }

    performAction(temporaryToken, reader, tokens, flush) {
      reader.readUntilCharsLast(false, "\r", "\n");
      flush(new TokenSharpDirective("#" + reader.buffer));
      return temporaryToken;
    }

  };

  CSharpParser = class CSharpParser extends SyntaxParser {
    init() {
      super.init();
      this.registerAnalyzers([new CppCommentAnalyzer(), new CppCommentPlusAnalyzer(), new SharpDirectiveAnalyzer(), new DelimiterAnalyzer()]);
      this.registerKeywords(["abstract", "as", "base", "bool", "break", "byte", "case", "catch", "char", "checked", "class", "const", "continue", "decimal", "default", "delegate", "do", "double", "else", "enum", "event", "explicit", "extern", "false", "finally", "fixed", "float", "for", "foreach", "goto", "if", "implicit", "in", "int", "interface", "internal", "is", "lock", "long", "namespace", "new", "null", "object", "operator", "out", "override", "params", "private", "protected", "public", "readonly", "ref", "return", "sbyte", "sealed", "short", "sizeof", "stackalloc", "static", "string", "struct", "switch", "this", "throw", "true", "try", "typeof", "uint", "ulong", "unchecked", "unsafe", "ushort", "using", "var", "virtual", "void", "volatile", "while"]);
      return this.registerOutlinedWords(["String", "Boolean", "Int32", "Int16", "Int64", "Double", "StringBuilder", "Object", "KeyValuePair", "List", "Array", "Dictionary", "HashSet"]);
    }

  };

  ParserFactory.Register(['cs', 'csharp', 'c#'], CSharpParser, "C#");

  CoffeeScriptParser = class CoffeeScriptParser extends SyntaxParser {
    init() {
      super.init();
      this.registerAnalyzers([new UnixCommentAnalyzer(), new DelimiterAnalyzer()]);
      return this.registerKeywords(["if", "then", "else", "unless", "and", "new", "return", "try", "catch", "finally", "throw", "or", "is", "isnt", "not", "break", "continue", "by", "of", "where", "when", "until", "for", "in", "while", "delete", "instanceof", "typeof", "switch", "super", "extends", "class", "case", "default", "do", "function", "with", "const", "let", "debugger", "enum", "export", "import", "native", "__extends", "__hasProp"]);
    }

  };

  ParserFactory.Register(['coffee', 'coffeescript'], CoffeeScriptParser, "Coffee Script");

  JavaParser = class JavaParser extends SyntaxParser {
    init() {
      super.init();
      this.registerAnalyzers([new CppCommentAnalyzer(), new CppCommentPlusAnalyzer(), new DelimiterAnalyzer()]);
      this.registerKeywords(["abstract", "assert", "boolean", "break", "byte", "case", "catch", "char", "class", "const", "continue", "default", "do", "double", "else", "enum", "extends", "final", "finally", "float", "for", "goto", "if", "implements", "import", "instanceof", "int", "interface", "internal", "is", "long", "native", "new", "package", "private", "protected", "public", "return", "short", "static", "strictfp", "super", "switch", "synchronized", "this", "throw", "throws", "transient", "try", "void", "volatile", "while"]);
      return this.registerOutlinedWords(["string", "int", "object"]);
    }

  };

  ParserFactory.Register(['java'], JavaParser, "Java");

  JavaScriptParser = class JavaScriptParser extends SyntaxParser {
    init() {
      super.init();
      this.registerAnalyzers([new CppCommentAnalyzer(), new CppCommentPlusAnalyzer(), new DelimiterAnalyzer()]);
      this.registerKeywords(["await", "break", "case", "catch", "class", "const", "continue", "debugger", "default", "delete", "do", "else", "export", "extends", "finally", "for", "function", "if", "import", "in", "instanceof", "let", "new", "return", "super", "switch", "this", "throw", "try", "typeof", "var", "void", "while", "with", "yield"]);
      return this.registerOutlinedWords(["Array", "Boolean", "Date", "Map", "Number", "Object", "Promise", "RegExp", "Set", "String"]);
    }

  };

  ParserFactory.Register(['js', 'javascript', 'mjs', 'cjs'], JavaScriptParser, "JavaScript");

  TypeScriptParser = class TypeScriptParser extends SyntaxParser {
    init() {
      super.init();
      this.registerAnalyzers([new CppCommentAnalyzer(), new CppCommentPlusAnalyzer(), new DelimiterAnalyzer()]);
      this.registerKeywords(["abstract", "any", "as", "asserts", "async", "await", "bigint", "boolean", "break", "case", "catch", "class", "const", "constructor", "continue", "declare", "default", "delete", "do", "else", "enum", "export", "extends", "false", "finally", "for", "from", "function", "if", "implements", "import", "in", "infer", "instanceof", "interface", "is", "keyof", "let", "module", "namespace", "never", "new", "null", "number", "object", "private", "protected", "public", "readonly", "require", "return", "satisfies", "static", "string", "super", "switch", "symbol", "this", "throw", "true", "try", "type", "typeof", "undefined", "unique", "unknown", "var", "void", "while"]);
      return this.registerOutlinedWords(["Array", "Map", "Promise", "Record", "Set", "String"]);
    }

  };

  ParserFactory.Register(['ts', 'typescript', 'tsx'], TypeScriptParser, "TypeScript");

  PythonParser = class PythonParser extends SyntaxParser {
    init() {
      super.init();
      this.registerAnalyzers([new UnixCommentAnalyzer(), new DelimiterAnalyzer()]);
      this.registerKeywords(["and", "as", "assert", "async", "await", "break", "class", "continue", "def", "del", "elif", "else", "except", "False", "finally", "for", "from", "global", "if", "import", "in", "is", "lambda", "None", "nonlocal", "not", "or", "pass", "raise", "return", "True", "try", "while", "with", "yield"]);
      return this.registerOutlinedWords(["dict", "float", "int", "list", "print", "set", "str", "tuple"]);
    }

  };

  ParserFactory.Register(['py', 'python'], PythonParser, "Python");

  GoParser = class GoParser extends SyntaxParser {
    init() {
      super.init();
      this.registerAnalyzers([new CppCommentAnalyzer(), new CppCommentPlusAnalyzer(), new DelimiterAnalyzer()]);
      this.registerKeywords(["break", "case", "chan", "const", "continue", "default", "defer", "else", "fallthrough", "for", "func", "go", "goto", "if", "import", "interface", "map", "package", "range", "return", "select", "struct", "switch", "type", "var"]);
      return this.registerOutlinedWords(["append", "bool", "byte", "error", "float64", "int", "make", "string"]);
    }

  };

  ParserFactory.Register(['go', 'golang'], GoParser, "Go");

  CParser = class CParser extends SyntaxParser {
    init() {
      super.init();
      this.registerAnalyzers([new CppCommentAnalyzer(), new CppCommentPlusAnalyzer(), new DelimiterAnalyzer()]);
      this.registerKeywords(["auto", "break", "case", "char", "const", "continue", "default", "do", "double", "else", "enum", "extern", "float", "for", "goto", "if", "inline", "int", "long", "register", "restrict", "return", "short", "signed", "sizeof", "static", "struct", "switch", "typedef", "union", "unsigned", "void", "volatile", "while", "_Bool", "_Complex", "_Imaginary"]);
      return this.registerOutlinedWords(["FILE", "NULL", "printf", "scanf", "size_t"]);
    }

  };

  ParserFactory.Register(['c', 'h'], CParser, "C");

  CppParser = class CppParser extends SyntaxParser {
    init() {
      super.init();
      this.registerAnalyzers([new CppCommentAnalyzer(), new CppCommentPlusAnalyzer(), new DelimiterAnalyzer()]);
      this.registerKeywords(["alignas", "alignof", "and", "asm", "auto", "bool", "break", "case", "catch", "char", "class", "const", "constexpr", "continue", "decltype", "default", "delete", "do", "double", "else", "enum", "explicit", "export", "extern", "false", "final", "float", "for", "friend", "goto", "if", "inline", "int", "long", "mutable", "namespace", "new", "noexcept", "nullptr", "operator", "or", "private", "protected", "public", "register", "return", "short", "signed", "sizeof", "static", "struct", "switch", "template", "this", "throw", "true", "try", "typedef", "typename", "union", "unsigned", "using", "virtual", "void", "volatile", "while"]);
      return this.registerOutlinedWords(["std", "string", "vector", "map", "set", "cout", "cin"]);
    }

  };

  ParserFactory.Register(['cpp', 'cxx', 'cc', 'hpp', 'hh'], CppParser, "C++");

  RustParser = class RustParser extends SyntaxParser {
    init() {
      super.init();
      this.registerAnalyzers([new CppCommentAnalyzer(), new CppCommentPlusAnalyzer(), new DelimiterAnalyzer()]);
      this.registerKeywords(["as", "async", "await", "break", "const", "continue", "crate", "dyn", "else", "enum", "extern", "false", "fn", "for", "if", "impl", "in", "let", "loop", "match", "mod", "move", "mut", "pub", "ref", "return", "self", "Self", "static", "struct", "super", "trait", "true", "type", "unsafe", "use", "where", "while"]);
      return this.registerOutlinedWords(["Option", "Result", "String", "Vec", "println", "format"]);
    }

  };

  ParserFactory.Register(['rs', 'rust'], RustParser, "Rust");

  BashParser = class BashParser extends SyntaxParser {
    init() {
      super.init();
      this.registerAnalyzers([new UnixCommentAnalyzer(), new DelimiterAnalyzer()]);
      this.registerKeywords(["case", "do", "done", "elif", "else", "esac", "fi", "for", "function", "if", "in", "select", "then", "time", "until", "while"]);
      return this.registerOutlinedWords(["echo", "export", "local", "printf", "readonly", "source"]);
    }

  };

  ParserFactory.Register(['sh', 'bash', 'zsh'], BashParser, "Bash");

  SqlLineCommentAnalyzer = class SqlLineCommentAnalyzer extends Analyzer {
    match(reader, temporaryToken, tokens) {
      var atLineStart, hasFirstDash, hasSecondDash;
      atLineStart = temporaryToken === null;
      hasFirstDash = reader.current() === '-';
      hasSecondDash = reader.fetchNext() === '-';
      return atLineStart && hasFirstDash && hasSecondDash;
    }

    performAction(temporaryToken, reader, tokens, flush) {
      reader.readUntilCharsLast(false, "\r", "\n");
      flush(new TokenComment("-" + reader.buffer));
      return temporaryToken;
    }

  };

  SqlParser = class SqlParser extends SyntaxParser {
    init() {
      super.init();
      this.registerAnalyzers([new SqlLineCommentAnalyzer(), new CppCommentPlusAnalyzer(), new DelimiterAnalyzer()]);
      this.registerKeywords(["all", "and", "as", "asc", "by", "case", "create", "delete", "desc", "distinct", "drop", "else", "exists", "from", "group", "having", "in", "insert", "into", "is", "join", "left", "like", "limit", "not", "null", "on", "or", "order", "right", "select", "set", "table", "then", "union", "update", "values", "when", "where"]);
      return this.registerOutlinedWords(["avg", "count", "max", "min", "sum"]);
    }

  };

  ParserFactory.Register(['sql'], SqlParser, "SQL");

  KotlinParser = class KotlinParser extends SyntaxParser {
    init() {
      super.init();
      this.registerAnalyzers([new CppCommentAnalyzer(), new CppCommentPlusAnalyzer(), new DelimiterAnalyzer()]);
      this.registerKeywords(["as", "break", "class", "continue", "do", "else", "false", "for", "fun", "if", "in", "interface", "is", "null", "object", "package", "return", "super", "this", "throw", "true", "try", "typealias", "val", "var", "when", "while"]);
      return this.registerOutlinedWords(["Any", "Array", "Int", "List", "Map", "String"]);
    }

  };

  ParserFactory.Register(['kt', 'kts', 'kotlin'], KotlinParser, "Kotlin");

  SwiftParser = class SwiftParser extends SyntaxParser {
    init() {
      super.init();
      this.registerAnalyzers([new CppCommentAnalyzer(), new CppCommentPlusAnalyzer(), new DelimiterAnalyzer()]);
      this.registerKeywords(["as", "break", "case", "class", "continue", "default", "defer", "do", "else", "enum", "extension", "false", "for", "func", "guard", "if", "import", "in", "init", "let", "nil", "protocol", "return", "self", "struct", "super", "switch", "throw", "throws", "true", "try", "var", "where", "while"]);
      return this.registerOutlinedWords(["Array", "Bool", "Double", "Int", "String"]);
    }

  };

  ParserFactory.Register(['swift'], SwiftParser, "Swift");

  PhpParser = class PhpParser extends SyntaxParser {
    init() {
      super.init();
      this.registerAnalyzers([new UnixCommentAnalyzer(), new CppCommentAnalyzer(), new CppCommentPlusAnalyzer(), new DelimiterAnalyzer()]);
      this.registerKeywords(["abstract", "array", "as", "break", "case", "class", "const", "continue", "default", "echo", "else", "elseif", "extends", "false", "for", "foreach", "function", "if", "implements", "include", "namespace", "new", "null", "private", "protected", "public", "require", "return", "static", "switch", "throw", "true", "try", "use", "var", "while"]);
      return this.registerOutlinedWords(["Exception", "PDO", "Stringable"]);
    }

  };

  ParserFactory.Register(['php', 'phtml'], PhpParser, "PHP");

  HtmlParser = class HtmlParser extends SyntaxParser {
    init() {
      super.init();
      this.registerAnalyzers([new DelimiterAnalyzer(['<', '>', '/', '=', '"', "'", '!', '-']), new DelimiterAnalyzer()]);
      return this.registerKeywords(["body", "class", "div", "head", "html", "id", "link", "meta", "script", "span", "style", "title"]);
    }

  };

  ParserFactory.Register(['html', 'htm', 'xhtml'], HtmlParser, "HTML");

  CssParser = class CssParser extends SyntaxParser {
    init() {
      super.init();
      this.registerAnalyzers([new CppCommentPlusAnalyzer(), new DelimiterAnalyzer()]);
      this.registerKeywords(["align-items", "background", "border", "color", "display", "font-family", "font-size", "grid", "margin", "padding", "position", "width"]);
      return this.registerOutlinedWords(["em", "px", "rem", "vh", "vw"]);
    }

  };

  ParserFactory.Register(['css', 'scss', 'less'], CssParser, "CSS");

  XmlParser = class XmlParser extends SyntaxParser {
    init() {
      super.init();
      this.registerAnalyzers([new DelimiterAnalyzer(['<', '>', '/', '=', '"', "'", '?', ':']), new DelimiterAnalyzer()]);
      return this.registerKeywords(["encoding", "version", "xml", "xmlns"]);
    }

  };

  ParserFactory.Register(['xml', 'xsd', 'svg'], XmlParser, "XML");

  JsonParser = class JsonParser extends SyntaxParser {
    init() {
      super.init();
      this.registerAnalyzers([new DelimiterAnalyzer()]);
      return this.registerKeywords(["false", "null", "true"]);
    }

  };

  ParserFactory.Register(['json'], JsonParser, "JSON");

  YamlParser = class YamlParser extends SyntaxParser {
    init() {
      super.init();
      this.registerAnalyzers([new UnixCommentAnalyzer(), new DelimiterAnalyzer()]);
      return this.registerKeywords(["false", "no", "null", "off", "on", "true", "yes"]);
    }

  };

  ParserFactory.Register(['yaml', 'yml'], YamlParser, "YAML");

  RubyParser = class RubyParser extends SyntaxParser {
    init() {
      super.init();
      this.registerAnalyzers([new UnixCommentAnalyzer(), new DelimiterAnalyzer()]);
      this.registerKeywords(["BEGIN", "END", "alias", "and", "begin", "break", "case", "class", "def", "defined", "do", "else", "elsif", "end", "ensure", "false", "for", "if", "module", "next", "nil", "not", "or", "redo", "rescue", "retry", "return", "self", "super", "private", "then", "true", "undef", "unless", "until", "when", "while", "yield", "__FILE__", "__LINE__"]);
      return this.registerOutlinedWords(["print"]);
    }

  };

  ParserFactory.Register(['rb', 'ruby'], RubyParser, "Ruby");

  Scheme = (function() {
    class Scheme {
      constructor(font1, size1) {
        this.font = font1;
        this.size = size1;
      }

      get(key) {
        if ((this.colors[key] != null)) {
          return this.colors[key];
        }
        return null;
      }

      set(key, description) {
        this.colors[key] = description;
        return this;
      }

    };

    Scheme.prototype.colors = new Object();

    return Scheme;

  }).call(this);

  SchemeFactoryController = class SchemeFactoryController extends Factory {
    Create(code, font = 'Courier New', size = 13) {
      var s;
      s = this.Find(code);
      return new s(font, size);
    }

    List() {
      var a, i, len, p, ref;
      a = [];
      ref = this.data;
      for (i = 0, len = ref.length; i < len; i++) {
        p = ref[i];
        a.push({
          code: p.code,
          name: p.title
        });
      }
      return a;
    }

  };

  SchemeFactory = new SchemeFactoryController();

  VisualStudioTenSch = class VisualStudioTenSch extends Scheme {
    constructor(font, size) {
      super(font, size);
      this.set('text-normal', new SchemeDescription());
      this.set('text-keyword', new SchemeDescription('#0000FF'));
      this.set('text-outlined', new SchemeDescription('#008080'));
      this.set('text-string', new SchemeDescription('#800000'));
      this.set('text-comment', new SchemeDescription('#008000'));
      this.set('text-directive', new SchemeDescription('#000080', 'bold'));
      this.set('text-specialSymbol', new SchemeDescription('#000000', 'bold'));
      this.set('bg', new SchemeDescription('#FFFFFF', 'solid'));
    }

  };

  SchemeFactory.Register('vs2010', VisualStudioTenSch, 'VS 2010');

  AptanaSch = class AptanaSch extends Scheme {
    constructor(font, size) {
      super(font, size);
      this.set('text-normal', new SchemeDescription('#9b6f46'));
      this.set('text-keyword', new SchemeDescription('#cda772'));
      this.set('text-outlined', new SchemeDescription('#7187a3'));
      this.set('text-string', new SchemeDescription('#8f9d69'));
      this.set('text-comment', new SchemeDescription('#615e64'));
      this.set('text-directive', new SchemeDescription('#c86952', 'bold'));
      this.set('text-specialSymbol', new SchemeDescription('#c86952', 'bold'));
      this.set('bg', new SchemeDescription('#141414', 'solid'));
    }

  };

  SchemeFactory.Register('aptana', AptanaSch, 'Aptana');

  GitHubLightSch = class GitHubLightSch extends Scheme {
    constructor(font, size) {
      super(font, size);
      this.set('text-normal', new SchemeDescription('#24292f'));
      this.set('text-keyword', new SchemeDescription('#cf222e'));
      this.set('text-outlined', new SchemeDescription('#0550ae'));
      this.set('text-string', new SchemeDescription('#0a3069'));
      this.set('text-comment', new SchemeDescription('#6e7781'));
      this.set('text-directive', new SchemeDescription('#8250df', 'bold'));
      this.set('text-specialSymbol', new SchemeDescription('#d1242f', 'bold'));
      this.set('bg', new SchemeDescription('#ffffff', 'solid'));
    }

  };

  SchemeFactory.Register('github-light', GitHubLightSch, 'GitHub Light');

  GitHubDarkSch = class GitHubDarkSch extends Scheme {
    constructor(font, size) {
      super(font, size);
      this.set('text-normal', new SchemeDescription('#c9d1d9'));
      this.set('text-keyword', new SchemeDescription('#ff7b72'));
      this.set('text-outlined', new SchemeDescription('#79c0ff'));
      this.set('text-string', new SchemeDescription('#a5d6ff'));
      this.set('text-comment', new SchemeDescription('#8b949e'));
      this.set('text-directive', new SchemeDescription('#d2a8ff', 'bold'));
      this.set('text-specialSymbol', new SchemeDescription('#ffa657', 'bold'));
      this.set('bg', new SchemeDescription('#0d1117', 'solid'));
    }

  };

  SchemeFactory.Register('github-dark', GitHubDarkSch, 'GitHub Dark');

  MonokaiSch = class MonokaiSch extends Scheme {
    constructor(font, size) {
      super(font, size);
      this.set('text-normal', new SchemeDescription('#f8f8f2'));
      this.set('text-keyword', new SchemeDescription('#f92672'));
      this.set('text-outlined', new SchemeDescription('#66d9ef'));
      this.set('text-string', new SchemeDescription('#e6db74'));
      this.set('text-comment', new SchemeDescription('#75715e'));
      this.set('text-directive', new SchemeDescription('#a6e22e', 'bold'));
      this.set('text-specialSymbol', new SchemeDescription('#fd971f', 'bold'));
      this.set('bg', new SchemeDescription('#272822', 'solid'));
    }

  };

  SchemeFactory.Register('monokai', MonokaiSch, 'Monokai');

  OneDarkSch = class OneDarkSch extends Scheme {
    constructor(font, size) {
      super(font, size);
      this.set('text-normal', new SchemeDescription('#abb2bf'));
      this.set('text-keyword', new SchemeDescription('#c678dd'));
      this.set('text-outlined', new SchemeDescription('#61afef'));
      this.set('text-string', new SchemeDescription('#98c379'));
      this.set('text-comment', new SchemeDescription('#5c6370'));
      this.set('text-directive', new SchemeDescription('#e5c07b', 'bold'));
      this.set('text-specialSymbol', new SchemeDescription('#56b6c2', 'bold'));
      this.set('bg', new SchemeDescription('#282c34', 'solid'));
    }

  };

  SchemeFactory.Register('one-dark', OneDarkSch, 'One Dark');

  SolarizedLightSch = class SolarizedLightSch extends Scheme {
    constructor(font, size) {
      super(font, size);
      this.set('text-normal', new SchemeDescription('#586e75'));
      this.set('text-keyword', new SchemeDescription('#859900'));
      this.set('text-outlined', new SchemeDescription('#268bd2'));
      this.set('text-string', new SchemeDescription('#2aa198'));
      this.set('text-comment', new SchemeDescription('#93a1a1'));
      this.set('text-directive', new SchemeDescription('#d33682', 'bold'));
      this.set('text-specialSymbol', new SchemeDescription('#b58900', 'bold'));
      this.set('bg', new SchemeDescription('#fdf6e3', 'solid'));
    }

  };

  SchemeFactory.Register('solarized-light', SolarizedLightSch, 'Solarized Light');

  SolarizedDarkSch = class SolarizedDarkSch extends Scheme {
    constructor(font, size) {
      super(font, size);
      this.set('text-normal', new SchemeDescription('#93a1a1'));
      this.set('text-keyword', new SchemeDescription('#859900'));
      this.set('text-outlined', new SchemeDescription('#268bd2'));
      this.set('text-string', new SchemeDescription('#2aa198'));
      this.set('text-comment', new SchemeDescription('#657b83'));
      this.set('text-directive', new SchemeDescription('#d33682', 'bold'));
      this.set('text-specialSymbol', new SchemeDescription('#cb4b16', 'bold'));
      this.set('bg', new SchemeDescription('#002b36', 'solid'));
    }

  };

  SchemeFactory.Register('solarized-dark', SolarizedDarkSch, 'Solarized Dark');

  DraculaSch = class DraculaSch extends Scheme {
    constructor(font, size) {
      super(font, size);
      this.set('text-normal', new SchemeDescription('#f8f8f2'));
      this.set('text-keyword', new SchemeDescription('#ff79c6'));
      this.set('text-outlined', new SchemeDescription('#8be9fd'));
      this.set('text-string', new SchemeDescription('#f1fa8c'));
      this.set('text-comment', new SchemeDescription('#6272a4'));
      this.set('text-directive', new SchemeDescription('#bd93f9', 'bold'));
      this.set('text-specialSymbol', new SchemeDescription('#ffb86c', 'bold'));
      this.set('bg', new SchemeDescription('#282a36', 'solid'));
    }

  };

  SchemeFactory.Register('dracula', DraculaSch, 'Dracula');

  IntelliJLightSch = class IntelliJLightSch extends Scheme {
    constructor(font, size) {
      super(font, size);
      this.set('text-normal', new SchemeDescription('#000000'));
      this.set('text-keyword', new SchemeDescription('#0033b3'));
      this.set('text-outlined', new SchemeDescription('#267f99'));
      this.set('text-string', new SchemeDescription('#067d17'));
      this.set('text-comment', new SchemeDescription('#8c8c8c'));
      this.set('text-directive', new SchemeDescription('#871094', 'bold'));
      this.set('text-specialSymbol', new SchemeDescription('#1750eb', 'bold'));
      this.set('bg', new SchemeDescription('#ffffff', 'solid'));
    }

  };

  SchemeFactory.Register('intellij-light', IntelliJLightSch, 'IntelliJ Light');

  NordSch = class NordSch extends Scheme {
    constructor(font, size) {
      super(font, size);
      this.set('text-normal', new SchemeDescription('#d8dee9'));
      this.set('text-keyword', new SchemeDescription('#81a1c1'));
      this.set('text-outlined', new SchemeDescription('#88c0d0'));
      this.set('text-string', new SchemeDescription('#a3be8c'));
      this.set('text-comment', new SchemeDescription('#616e88'));
      this.set('text-directive', new SchemeDescription('#b48ead', 'bold'));
      this.set('text-specialSymbol', new SchemeDescription('#ebcb8b', 'bold'));
      this.set('bg', new SchemeDescription('#2e3440', 'solid'));
    }

  };

  SchemeFactory.Register('nord', NordSch, 'Nord');

  XcodeLightSch = class XcodeLightSch extends Scheme {
    constructor(font, size) {
      super(font, size);
      this.set('text-normal', new SchemeDescription('#1f1f24'));
      this.set('text-keyword', new SchemeDescription('#ad3da4'));
      this.set('text-outlined', new SchemeDescription('#0f68a0'));
      this.set('text-string', new SchemeDescription('#d12f1b'));
      this.set('text-comment', new SchemeDescription('#6c7986'));
      this.set('text-directive', new SchemeDescription('#78492a', 'bold'));
      this.set('text-specialSymbol', new SchemeDescription('#205d95', 'bold'));
      this.set('bg', new SchemeDescription('#ffffff', 'solid'));
    }

  };

  SchemeFactory.Register('xcode-light', XcodeLightSch, 'Xcode Light');

  Painter = (function() {
    class Painter {
      constructor(canvas, settings1, scheme1) {
        this.settings = settings1;
        this.scheme = scheme1;
        this.ctx = canvas.getContext('2d');
      }

      withChild(child) {
        this.child = child;
        return this;
      }

      scheme() {
        return this.scheme;
      }

      measure() {
        var h, iH, iW, w;
        w = 0;
        h = 0;
        if (this.w > 0) {
          w = this.w;
        }
        if (this.h > 0) {
          h = this.h;
        }
        if (this.child !== null) {
          [iW, iH] = this.child.measure();
          if (w < iW) {
            w = iW;
          }
          h += iH;
        }
        w += this.pL + this.pR;
        h += this.pT + this.pB;
        return [w, h];
      }

      paint() {
        this.paintMe(0, 0);
        if ((this.child != null)) {
          this.child.paintMe(this.pL, this.pT);
        }
        return null;
      }

      paintMe(x, y) {
        return null;
      }

    };

    Painter.prototype.settings = null;

    Painter.prototype.child = null;

    Painter.prototype.w = -1;

    Painter.prototype.h = -1;

    Painter.prototype.pT = 0;

    Painter.prototype.pB = 0;

    Painter.prototype.pL = 0;

    Painter.prototype.pR = 0;

    return Painter;

  }).call(this);

  PaintersBase = class PaintersBase extends Painter {
    constructor(canvas, params, settings, scheme) {
      super(canvas, settings, scheme);
      this.params = params;
      this.initialize();
    }

    get(key, defo = null) {
      var c;
      if ((this.params[key] != null)) {
        return this.params[key];
      }
      c = this.scheme.get(key);
      if ((c != null)) {
        return c.color;
      }
      return defo;
    }

    setIfNull(key, val) {
      if (this.params[key] == null) {
        this.params[key] = val;
      }
      return true;
    }

    initialize() {
      return null;
    }

  };

  PainterFactoryController = class PainterFactoryController extends Factory {
    constructor() {
      super();
      this.processParams = function(params) {
        var a, f, i, kv, len, p, s;
        a = params.trim().split(',');
        s = '{';
        f = true;
        for (i = 0, len = a.length; i < len; i++) {
          p = a[i];
          kv = p.trim().split(':', 2);
          if (kv.length !== 2) {
            continue;
          }
          if (f) {
            f = false;
          } else {
            s += ',';
          }
          s += '"' + kv[0] + '":"' + kv[1] + '"';
        }
        s += '}';
        return s;
      };
    }

    Create(canvas, initString, context, scheme) {
      var a, code, p, par;
      a = initString.split('=>', 2);
      if (a.length < 1) {
        return null;
      }
      code = a[0].trim().toLowerCase();
      p = this.Find(code);
      if (p == null) {
        return null;
      }
      par = {};
      if (a.length > 1) {
        par = JSON.parse(this.processParams(a[1]));
      }
      return new p(canvas, par, context, scheme);
    }

  };

  PainterFactory = new PainterFactoryController();

  ServicePainterBorder = (function() {
    class ServicePainterBorder extends PaintersBase {
      initialize() {
        var r;
        this.setIfNull('radius', 0);
        this.setIfNull('width', 1);
        r = parseInt(this.get('radius'));
        if (r <= this.defRad) {
          r = this.defRad + this.defOffset;
        } else {
          r = r + this.defOffset;
        }
        this.pL = r;
        this.pT = r;
        this.pB = r;
        return this.pR = r;
      }

      paintMe(x, y) {
        var bg, cH, cW, o, o2, radius, x1, x2, y1, y2;
        cW = 0;
        cH = 0;
        if ((this.child != null)) {
          [cW, cH] = this.child.measure();
        }
        o = this.defOffset;
        o2 = 2 * this.defOffset;
        x1 = x + o;
        y1 = y + o;
        x2 = x + cW + this.pL + this.pR - o2;
        y2 = y + cH + this.pT + this.pB - o2;
        radius = parseInt(this.get('radius'));
        this.ctx.beginPath();
        this.ctx.moveTo(x1 + radius, y1);
        this.ctx.lineTo(x2 - radius, y1);
        this.ctx.quadraticCurveTo(x2, y1, x2, y1 + radius);
        this.ctx.lineTo(x2, y2 - radius);
        this.ctx.quadraticCurveTo(x2, y2, x2 - radius, y2);
        this.ctx.lineTo(x1 + radius, y2);
        this.ctx.quadraticCurveTo(x1, y2, x1, y2 - radius);
        this.ctx.lineTo(x1, y1 + radius);
        this.ctx.quadraticCurveTo(x1, y1, x1 + radius, y1);
        this.ctx.closePath();
        this.ctx.strokeStyle = this.get('fg', '#000000');
        this.ctx.lineWidth = parseInt(this.get('width'));
        this.ctx.stroke();
        bg = this.get('bg', 'transparent');
        if (bg !== 'transparent') {
          this.ctx.fillStyle = bg;
          this.ctx.fill();
        }
        return null;
      }

    };

    ServicePainterBorder.prototype.defRad = 5;

    ServicePainterBorder.prototype.defOffset = 1;

    return ServicePainterBorder;

  }).call(this);

  PainterFactory.Register('border', ServicePainterBorder);

  CodePainter = class CodePainter extends Painter {
    constructor(canvas, tokens, scheme, conf) {
      super(canvas, conf, scheme);
      this.tokens = tokens;
      //@settings
      [this.w, this.h] = this.calculate();
    }

    withChild(child) {
      this.child = null;
      return this;
    }

    calculate() {
      var currH, currW, h, i, len, maxH, maxW, ref, t, w;
      maxW = 0;
      maxH = 0;
      currH = 0;
      currW = 0;
      ref = this.tokens;
      for (i = 0, len = ref.length; i < len; i++) {
        t = ref[i];
        [w, h] = this.measureToken(t);
        currW += w;
        if (t instanceof TokenNewLine) {
          if (currH < h) {
            currH = h;
          }
          maxH += currH;
          if (maxW < currW) {
            maxW = currW;
          }
          currH = 0;
          currW = 0;
          continue;
        }
      }
      maxH += currH;
      if (maxW < currW) {
        maxW = currW;
      }
      if (maxW < this.settings.minWidth) {
        maxW = this.settings.minWidth;
      }
      return [maxW, maxH];
    }

    prepareContextForText(tokenCode) {
      var shDescription;
      shDescription = this.scheme.get('text-' + tokenCode);
      if (shDescription === null) {
        shDescription = this.scheme.get('text-normal');
      }
      if (shDescription === null) {
        shDescription = new SchemeDescription();
      }
      this.ctx.textBaseline = 'top';
      this.ctx.textAlign = 'left';
      this.ctx.font = shDescription.param + " " + this.scheme.size + "px " + this.scheme.font;
      this.ctx.fillStyle = shDescription.color;
      return this.scheme.size;
    }

    measureToken(t) {
      var c, dim, s;
      c = t.content;
      if (t instanceof TokenSpace) {
        c = " ";
      }
      s = this.prepareContextForText(t.code());
      dim = this.ctx.measureText(c);
      return [Math.round(dim.width), s];
    }

    drawToken(t, x, y) {
      var c, dim, s;
      c = t.content;
      if (t instanceof TokenSpace) {
        c = " ";
      }
      s = this.prepareContextForText(t.code());
      dim = this.ctx.measureText(c);
      this.ctx.fillText(c, x, y);
      return [Math.round(dim.width), s];
    }

    paintMe(x, y) {
      var cH, h, i, len, offX, offY, ref, t, w;
      offX = x;
      offY = y;
      cH = 0;
      ref = this.tokens;
      for (i = 0, len = ref.length; i < len; i++) {
        t = ref[i];
        [w, h] = this.drawToken(t, offX, offY);
        if (cH < h) {
          cH = h;
        }
        offX += w;
        if (t instanceof TokenNewLine) {
          offX = x;
          offY += cH;
          continue;
        }
      }
      return null;
    }

  };

  SnipController = class SnipController {
    constructor() {
      this.transform = function(ext, text, scheme, painters = [], context = null) {
        var c, cont, dbg, h, i, len, p, ps, ratio, sh, tokens, tp, w;
        cont = context;
        if (cont == null) {
          cont = this.mergeSettings(cont);
        }
        tokens = ParserFactory.Parse(ext, text);
        if (tokens === null) {
          console.log("snip2me: extension `" + ext + "` is not supported");
          return null;
        }
        dbg = document.getElementById('pre4debug');
        if ((dbg != null)) {
          dbg.innerHTML = tokens.Print();
        }
        c = document.createElement("canvas");
        sh = SchemeFactory.Create(scheme, cont.font, cont.size);
        if (sh === null) {
          console.log("snip2me: scheme `" + scheme + "` is not supported");
          return null;
        }
        p = new CodePainter(c, tokens, sh, cont);
        for (i = 0, len = painters.length; i < len; i++) {
          ps = painters[i];
          tp = PainterFactory.Create(c, ps, cont, sh);
          if (tp == null) {
            break;
          }
          tp.withChild(p);
          p = tp;
        }
        [w, h] = p.measure();
        ratio = ((cont.pixelRatio != null) && cont.pixelRatio > 0) ? cont.pixelRatio : 1;
        c.width = w * ratio;
        c.height = h * ratio;
        if (ratio !== 1) {
          c.getContext('2d').scale(ratio, ratio);
        }
        p.paint();
        return c.toDataURL();
      };
      this.getPaintersFromElement = function(el) {
        var a, arr, attr, code, i, j, len, len1, r, ref, v;
        code = 'data-snip-painter-';
        arr = [];
        ref = el.attributes;
        for (i = 0, len = ref.length; i < len; i++) {
          attr = ref[i];
          if (attr.name.indexOf(code) !== 0) {
            continue;
          }
          v = {};
          v.num = parseInt(attr.name.substring(code.length));
          v.val = attr.value;
          arr.push(v);
        }
        arr.sort(function(a, b) {
          return b.num - a.num;
        });
        r = [];
        for (j = 0, len1 = arr.length; j < len1; j++) {
          a = arr[j];
          r.push(a.val);
        }
        return r;
      };
      this.transformElement = function(el) {
        var ext, gist, painters, sch;
        ext = el.getAttribute('data-snip-lang');
        sch = el.getAttribute('data-snip-scheme');
        gist = el.getAttribute('data-snip-gist');
        painters = this.getPaintersFromElement(el);
        if (gist == null) {
          return this.transformNonGist(el, ext, sch, painters);
        } else {
          return this.transformGist(el, ext, sch, gist, painters);
        }
      };
      this.transformNonGist = function(elem, ext, sch, painters) {
        var img, uri;
        uri = this.transform(ext, elem.childNodes[0].nodeValue, sch, painters, this.readConfFromElement(elem));
        if (uri === null) {
          return false;
        }
        img = uri.toImg();
        elem.parentNode.replaceChild(img, elem);
        return true;
      };
      this.transformGist = function(el, ext, sch, gist, painters) {
        return false;
      };
      this.findAllElements = function(attribute) {
        var allElements, elem, i, len, matchingElements;
        matchingElements = [];
        allElements = document.getElementsByTagName('*');
        for (i = 0, len = allElements.length; i < len; i++) {
          elem = allElements[i];
          if ((elem.getAttribute(attribute) != null)) {
            matchingElements.push(elem);
          }
        }
        return matchingElements;
      };
      this.defaults = {
        minWidth: 0,
        font: 'Courier New',
        size: 12,
        pixelRatio: 2
      };
      this.readConfFromElement = function(el) {
        var attr, code, i, len, obj, ref;
        code = 'data-snip-conf-';
        obj = {};
        ref = el.attributes;
        for (i = 0, len = ref.length; i < len; i++) {
          attr = ref[i];
          if (attr.name.indexOf(code) !== 0) {
            continue;
          }
          obj[attr.name.substring(code.length).dashToCamel()] = attr.value;
        }
        return this.mergeSettings(obj);
      };
      this.mergeSettings = function(obj) {
        if ((obj != null) && (obj.minwidth != null) && (obj.minWidth == null)) {
          obj.minWidth = obj.minwidth;
        }
        return this.mergeObjs(this.defaults, obj);
      };
      this.isInt = function(n) {
        return typeof n === 'number' && n % 1 === 0;
      };
      this.mergeObjs = function(obj1, obj2) {
        var error, i, len, obj3, p, ref, v;
        obj3 = {};
        ref = Object.keys(obj1);
        for (i = 0, len = ref.length; i < len; i++) {
          p = ref[i];
          v = null;
          try {
            if (obj1[p].constructor === Object) {
              v = this.mergeObjs(obj1[p], obj2[p]);
            } else {
              if ((obj2 != null) && (obj2[p] != null)) {
                v = obj2[p];
              } else {
                v = obj1[p];
              }
            }
          } catch (error1) {
            error = error1;
            v = obj1[p];
          }
          if (this.isInt(obj1[p])) {
            obj3[p] = parseInt(v);
          } else {
            obj3[p] = v;
          }
        }
        return obj3;
      };
      this.isElement = function(o) {
        if ((typeof HTMLElement) === "object") {
          return o instanceof HTMLElement;
        }
        return o && (typeof o) === "object" && o.nodeType === 1 && (typeof o.nodeName) === "string";
      };
      this.canRun = false;
      this.runChecked = false;
      this.checkVersions = function(browser, version) {
        switch (browser) {
          case "opera":
            return version >= 12.1;
          case "chrome":
            return version >= 22.0;
          case "safari":
            return version >= 5.1;
          case "firefox":
            return version >= 15.0;
          case "msie":
            return version >= 9.0;
          default:
            return true;
        }
      };
    }

    me() {
      var e, elems, i, len;
      if (!this.isCompatible()) {
        return false;
      }
      elems = this.findAllElements('data-snip-lang');
      for (i = 0, len = elems.length; i < len; i++) {
        e = elems[i];
        this.transformElement(e);
      }
      return true;
    }

    compile(element) {
      if (!this.isCompatible()) {
        return false;
      }
      if (this.isElement(element)) {
        return this.transformElement(element);
      } else {
        return this.transform.apply(this, arguments);
      }
    }

    setSettings(settings) {
      return this.defaults = this.mergeSettings(settings);
    }

    isCompatible() {
      var M, N, appVersion, browser, tem, ua, version;
      if (this.runChecked) {
        return this.canRun;
      }
      this.runChecked = true;
      N = (typeof navigator !== "undefined" && navigator !== null) && (navigator.appName != null) ? navigator.appName : 'unknown';
      ua = (typeof navigator !== "undefined" && navigator !== null) && (navigator.userAgent != null) ? navigator.userAgent : '';
      tem = null;
      M = ua.match(/(opera|chrome|safari|firefox|msie)\/?\s*(\.?\d+(\.\d+)*)/i);
      if (M && (tem = ua.match(/version\/([\.\d]+)/i)) !== null) {
        M[2] = tem[1];
      }
      if ((M != null) && (M[1] != null) && (M[2] != null)) {
        M = [M[1], M[2]];
      } else {
        appVersion = (typeof navigator !== "undefined" && navigator !== null) && (navigator.appVersion != null) ? navigator.appVersion : '0';
        M = [N, appVersion, '-?'];
      }
      browser = (M[0] != null) ? M[0].toLowerCase() : 'unknown';
      version = parseFloat(M[1]);
      if (isNaN(version)) {
        version = 0;
      }
      this.canRun = this.checkVersions(browser, version);
      if (!this.canRun) {
        console.log("snip2me is not compatible with current browser");
      }
      return this.canRun;
    }

    parsers() {
      return ParserFactory.List();
    }

    schemes() {
      return SchemeFactory.List();
    }

  };

  root = typeof exports !== "undefined" && exports !== null ? exports : this;

  root.snip2 = new SnipController();

  autoLoader = function() {
    if ((root.snip != null)) {
      root.snip2.setSettings.apply(root.snip2, [root.snip]);
    }
    return root.snip2.me.apply(root.snip2);
  };

  if (window.addEventListener) {
    window.addEventListener("load", autoLoader, false);
  } else {
    window.attachEvent("onload", autoLoader);
  }

}).call(this);
